import { settingsFeature } from './settings.reducer';

export const {
  selectSettingsState,
  selectIsOpen,
  selectActiveSection,
  selectLanguage,
  selectNotifications,
  selectWidgets,
} = settingsFeature;
